import { LogoStatusPipe } from './logo-status.pipe';

describe('LogoStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new LogoStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
